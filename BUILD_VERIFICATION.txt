APIP specification package verification — v2.3
Date: 2026-09-03

Reference scaffold:
- ./verify.sh: PASS (runs everything below, in order)
- cd reference && python -m unittest discover -s tests -v:
  PASS (264 tests on 2026-09-03; count is the live suite — the v2.1.1/v2.2
  adversarial audits added tests, so any earlier "187" is stale).
  NOTE the product package (src/apip) is pytest-run: the same command from
  the repo root discovers 0 tests (unittest does not collect pytest fixtures);
  the reproduction for the product suite is
  .venv-apip/bin/python -m pytest tests/  -> 190 passed.
- no-AI conformance gate (docs/28): PASS (CI-enforced, see tests/
  test_no_ai_conformance.py — dependency allowlist, AI package ban list,
  inference-call-shape scan, ApipRng-only entropy, sockets-blocked run)
- schema conformance (v2.2): PASS — every emitted artifact validated
  against the shipped JSON schemas by the dependency-free validator
  (tests/test_schema_conformance.py), cross-checked with the reference
  `jsonschema` implementation when installed
- dry-run evaluation: PASS (6 synthetic indicators, SHADOW mode,
  reference clock pinned by [replay].reference_now — byte-replay exact)
  - exact-FQDN C2 -> L4 domain block (dns_nxdomain, shadow)
  - shared-cloud IP -> L0 observe (identity-unsafe, context evidence
    below floors)
  - dedicated-IP C2 -> L5 IP deny (shadow)
  - first-seen DGA-like domain (single behavioral family) -> L0 OBSERVE
  - C2 on shared CDN (2 behavioral families + external) -> L2 pair
    rate-limit WITH drawn ceiling, rule compiled as http.host +
    detection_filter with ceiling in metadata
  - evidence payload smuggling points/origin/family -> NO_ACTION (inert)
- BD-1 behavioral detector wired into the CLI (`--events`): detections
  fold into matching indicators as local behavioral evidence through the
  same weight table, caps, and corroboration lattice as any evidence
- docs/25 L1 client-impact budget ENFORCED (v2.2 follow-up):
  max_challenged_transaction_fraction_per_hour counts every
  proxy_challenge against floor(fraction x measured volume); overflow
  ALARMS and AUTO-REVERTS to L0 (client_impact_budget_exceeded /
  challenge_auto_reverted_to_L0). Denominator is explicit operator
  input ([measurement].interactive_transactions_per_hour or CLI
  --transactions-per-hour); fraction set + unmeasured -> allowance 0
  (fail closed). Survivors deterministic by (policy_version, id);
  integer fixed-point arithmetic; non-challenge rungs never counted;
  reversion emits no enforcement artifact. Pinned by
  tests/test_independent_audit.py::ClientImpactBudgetTests (8 tests,
  incl. CLI end-to-end). The offline scaffold enforces the budget
  per batch against the full measured-hour allowance; cross-batch
  hourly tracking requires the live deployment the scaffold
  deliberately is not.

v2.2 independent-audit closure (all pinned in
tests/test_independent_audit.py + tests/test_schema_conformance.py):
- artifact-injection class closed at three boundaries: ingest charset
  validation (ids/sources/kinds/timestamps), selector-gate refusal,
  compile-time validation/escaping in both exporters. Demonstrated
  injection PoC (indicator id -> suricata metadata rule injection)
  now fails closed at load: PASS
- corroboration kinds are claims, verified against derived upstream
  provenance (one feed self-asserting two_curated_sources no longer
  reaches L5; reseller chains count once): PASS
- duplicate-record scoring collapse before the evidence envelope
  (dedup-then-cap ordering preserves exculpatory evidence): PASS
- canonical allowlist matching (case/trailing-dot/whitespace//32
  spellings all match; wider prefixes deliberately never match a host):
  PASS
- allowlist expires_at enforced against the replay clock; governed
  entries (owner+ticket) required at load: PASS
- blast-radius budget enforced (max_new_auto_actions_per_batch; overflow
  demotes to OBSERVE with a named reason): PASS
- authorized_prefixes loadable from [authorization] policy section: PASS
- thread-safe capture writer/counter/store (exact cap under 6-thread
  contention, no closed-file exceptions): PASS
- hostile Content-Length refused (400) or clamped; 30s handler timeout:
  PASS
- IPv6 cross-format handle stability ([v6]:port accepted, all spellings
  canonicalize, zone ids rejected): PASS
- emitted decisions conform to schemas/decision.schema.json (list-form
  randomization + attribution_refs + epoch draw field now in schema):
  PASS (6/6, both validators)
- unicode/invisible-character/NUL id injection rejected at load: PASS

Data/interface validation:
- JSON parse: PASS for all JSON schemas, sources.json, MANIFEST.json,
  and JSON examples
- OpenAPI YAML parse: PASS (PyYAML)
- examples/policy.toml TOML parse: PASS
- MANIFEST.json: 166 files hashed (regenerated 2026-09-03 against the git
  tree — every tracked file except MANIFEST.json itself, which is
  self-excluded); re-verified SHA-256 over all 166 paths, all match this
  tree. Verify with:
    python3 -c "import json,hashlib as h;m=json.load(open('MANIFEST.json'));\
b=[f['path'] for f in m['files'] if h.sha256(open(f['path'],'rb').read()).hexdigest()!=f['sha256']];\
print('DRIFT' if b else f'OK {len(m[\"files\"])} files')"
  (the earlier "88 files" figure was a v2.2 snapshot that had drifted as the
  product package, deploy/, and the lab driver were added in v2.3.)

No-AI conformance (docs/28) — CI-ENFORCED via tests/test_no_ai_conformance.py,
run inside verify.sh:
- dependency allowlist: security-bearing modules import only the
  deterministic stdlib allowlist (`threading` added in v2.2 for locks
  only — no timers, no entropy): PASS
- AI package ban list: zero imports anywhere: PASS
- static inference-call-shape scan: zero occurrences: PASS
- no `random` module usage; all draws via recorded ApipRng: PASS
- pyproject declares zero runtime dependencies: PASS
- full evaluation path executes with sockets blocked: PASS
- AI-evidence invariance regression (byte-identical decisions with and
  without annotation records): PASS
- attribution-output isolation (decisions byte-identical with and
  without attribution records; engine never auto-attaches refs): PASS

Lab:
- lab/run.sh: PASS (7/7 scenarios; loopback-only; no enforcement)

CI:
- .github/workflows/verify.yml runs verify.sh + the lab on push across
  Python 3.11/3.12/3.13

This verification proves the included scaffold behaves as described. It
is not a production security certification and does not validate any
future live adapter.
